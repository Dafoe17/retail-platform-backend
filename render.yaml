services:
  - type: web
    name: retail-platform-api
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn core.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
